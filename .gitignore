rbnx-build/
rbnx-boot/
__pycache__/
*.pyc
.venv/
# Runtime logs and editor/backup artifacts must not be committed.
logs/
*.log
*.bak
*.swp

# Vendored Livox sources (build input for the container image) — downloaded
# locally, never committed. Keep only the vendor/README.md.
container/vendor/*
!container/vendor/README.md
